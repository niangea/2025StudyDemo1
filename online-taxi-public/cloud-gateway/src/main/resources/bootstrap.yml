spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: devId
        file-extension: yaml
  application:
    name: cloud-gateway
  profiles:
    active: dev

